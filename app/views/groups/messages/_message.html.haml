.message
  .message__info
    .message__info--name
      = message.user.name
    .message__info--date
      = message.created_at
  .message__body
    = message.comment
  .message__image
    = image_tag message.image if message.image.present?
